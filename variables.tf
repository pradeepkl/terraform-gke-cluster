# terraform.tfvars

project_id   = "your-gcp-project-id"  # Replace with your GCP project ID
cluster_name = "optum-gke-cluster"
network      = "default"              # Replace if using custom VPC
subnetwork   = "default"              # Replace if using custom subnet
environment  = "production"